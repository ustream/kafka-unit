tasks.install.doLast {
    copy {
        from 'build/poms'
        into 'build/poms'
        include 'pom-default.xml'
        rename ('pom-default.xml', project.name + '-' + project.version + '.pom')
    }
}
